---
phase: 04-business-flows
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/prompts.yaml
autonomous: true

must_haves:
  truths:
    - "Un cliente que dice 'quiero tasar mi propiedad' recibe preguntas guiadas (dirección, tipo, m², antigüedad) y queda registrado como lead de captación en GHL"
    - "Un cliente que dice 'quiero vender mi casa' recibe info del servicio de venta y queda registrado como lead vendedor en GHL"
    - "Un cliente que dice 'quiero poner en alquiler' recibe info del servicio de administración y queda registrado como lead en GHL"
    - "Los tres flujos usan registrar_lead_ghl con operacion diferenciada (tasacion, captacion_venta, captacion_alquiler) y resumen descriptivo"
  artifacts:
    - path: "config/prompts.yaml"
      provides: "Three new flow blocks for tasación, venta, and poner en alquiler"
      contains: "Flujo.*Tasación"
  key_links:
    - from: "config/prompts.yaml"
      to: "agent/tools.py::registrar_lead_ghl"
      via: "Claude reads prompt → calls tool with operacion param"
      pattern: "registrar_lead_ghl.*operacion.*tasacion"
---

<objective>
Add three guided business conversation flows (tasación, venta, poner en alquiler) to the system prompt so Claude can guide clients through each flow and register them as leads in GHL.

Purpose: The qualification list (step 1) already has op_tasacion, op_vender, op_poner_alquiler IDs but Claude has no instructions on what to do when they're selected. These flow blocks tell Claude exactly what questions to ask and how to register the lead.

Output: Updated config/prompts.yaml with three new flow sections.
</objective>

<execution_context>
@C:/Users/Usuario/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Usuario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-business-flows/04-RESEARCH.md
@config/prompts.yaml
@agent/tools.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tasación, venta, and alquiler flow blocks to system prompt</name>
  <files>config/prompts.yaml</files>
  <action>
Add three new flow sections to config/prompts.yaml, inserting them AFTER the existing "## Agendar visita" section and BEFORE the "## Horario" section. Each flow block defines:

**Flow block 1: "Flujo Tasación"**
- Trigger: op_tasacion list ID, or phrases like "quiero tasar", "saber cuánto vale", "valuar"
- Steps: explain service briefly → ask dirección → ask tipo → ask m² (optional) → ask antigüedad (optional) → call registrar_lead_ghl
- registrar_lead_ghl params: operacion="tasacion", tipo_propiedad=[collected], zona=[from dirección], resumen="TASACIÓN SOLICITADA: [tipo] en [dirección] — aprox [m²] m², [antigüedad] años"
- nombre: use from history if available, otherwise pass "Cliente WhatsApp" — do NOT ask for name
- After registration: confirm and ask if there's anything else
- Register after collecting minimum dirección + tipo (m² and antigüedad are nice-to-have, not blockers)
- UNA pregunta a la vez. If client gives multiple data points in one message, accept all and skip already-answered steps.

**Flow block 2: "Flujo Vender mi propiedad"**
- Trigger: op_vender list ID, or "quiero vender", "poner en venta", "vender mi casa/depto"
- Steps: explain service (tasación + publicación + visitas + cierre) → ask dirección → ask tipo → ask m² (optional) → optionally ask if they have a price in mind → call registrar_lead_ghl
- registrar_lead_ghl params: operacion="captacion_venta", resumen="VENTA: cliente quiere poner en venta [tipo] en [dirección] — aprox [m²] m²[, precio estimado: X si lo dio]"
- Same nombre rule as tasación
- After registration: confirm and mention an advisor will coordinate a visit

**Flow block 3: "Flujo Poner en alquiler"**
- Trigger: op_poner_alquiler list ID, or "poner en alquiler", "alquilar mi propiedad", "administrar mi propiedad"
- Steps: explain rental management service (finding tenants, collecting rent, managing contracts) → ask dirección → ask tipo → ask m² (optional) → call registrar_lead_ghl
- registrar_lead_ghl params: operacion="captacion_alquiler", resumen="ALQUILER: cliente quiere poner en alquiler [tipo] en [dirección] — aprox [m²] m²"
- Same nombre rule
- After registration: confirm and mention advisor contact

**Critical rules for ALL three flows:**
- ALWAYS pass telefono from context to registrar_lead_ghl
- nombre: check conversation history first; if not found use "Cliente WhatsApp" — NEVER ask for name in these flows (phone is enough for follow-up)
- Email is NOT required — do NOT ask for email in tasación/venta/alquiler flows
- Register after dirección + tipo minimum to reduce drop-off
- m² and antigüedad are optional — if client says "no sé", skip and note in resumen
- If client provides all data in one message, take it all and register immediately

Write in Spanish (matching the rest of prompts.yaml). Use the same markdown heading style (##) as existing sections.
  </action>
  <verify>
Read config/prompts.yaml and confirm:
1. Three new sections exist: "Flujo.*Tasación", "Flujo.*Vender", "Flujo.*Poner en alquiler"
2. Each references registrar_lead_ghl with correct operacion value
3. Each specifies nombre="Cliente WhatsApp" default
4. No email requirement mentioned in any of the three flows
5. Minimum data requirement is dirección + tipo
6. File parses correctly: `python -c "import yaml; yaml.safe_load(open('config/prompts.yaml','r',encoding='utf-8'))"`
  </verify>
  <done>
Three flow blocks added to prompts.yaml. Each flow instructs Claude on what questions to ask (one at a time), what params to pass to registrar_lead_ghl, and what confirmation message to send. The flows differentiate via operacion param: tasacion, captacion_venta, captacion_alquiler.
  </done>
</task>

</tasks>

<verification>
1. `python -c "import yaml; yaml.safe_load(open('config/prompts.yaml','r',encoding='utf-8'))"` — YAML parses without error
2. Grep for "tasacion" in prompts.yaml — flow block exists
3. Grep for "captacion_venta" in prompts.yaml — flow block exists
4. Grep for "captacion_alquiler" in prompts.yaml — flow block exists
5. Verify the three new sections are positioned after "Agendar visita" and before "Horario"
</verification>

<success_criteria>
- config/prompts.yaml contains three new guided flow blocks (tasación, venta, poner en alquiler)
- Each flow specifies the exact operacion value for registrar_lead_ghl
- Each flow collects dirección + tipo as minimum, with m²/antigüedad optional
- No email or nombre required in any of the three flows (phone from context + "Cliente WhatsApp" default)
- YAML file parses correctly
</success_criteria>

<output>
After completion, create `.planning/phases/04-business-flows/04-01-SUMMARY.md`
</output>
