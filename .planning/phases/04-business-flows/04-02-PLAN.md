---
phase: 04-business-flows
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - agent/tools.py
  - agent/brain.py
  - config/prompts.yaml
autonomous: true

must_haves:
  truths:
    - "Un cliente puede decir 'empezar de nuevo' y su historial se borra, recibiendo la lista de calificación inicial como si fuera un cliente nuevo"
    - "Un cliente que dice 'quiero buscar otra cosa' NO pierde su historial — solo vuelve al paso correspondiente del flujo"
    - "Después de reiniciar, la lista de propiedades de sesión también se limpia (no quedan propiedades stale para agendar visita)"
  artifacts:
    - path: "agent/tools.py"
      provides: "reiniciar_conversacion function + TOOLS_DEFINITION entry"
      contains: "reiniciar_conversacion"
    - path: "agent/brain.py"
      provides: "reiniciar_conversacion wired in _ejecutar_herramienta dispatcher"
      contains: "reiniciar_conversacion"
    - path: "config/prompts.yaml"
      provides: "Restart flow instructions with trigger phrase list"
      contains: "Reinicio de conversación"
  key_links:
    - from: "agent/brain.py::_ejecutar_herramienta"
      to: "agent/tools.py::reiniciar_conversacion"
      via: "elif branch in dispatcher"
      pattern: "reiniciar_conversacion"
    - from: "agent/tools.py::reiniciar_conversacion"
      to: "agent/memory.py::limpiar_historial"
      via: "async function call"
      pattern: "limpiar_historial"
    - from: "agent/tools.py::reiniciar_conversacion"
      to: "agent/session.py::_cache"
      via: "dict delete for phone key"
      pattern: "_cache.*telefono"
---

<objective>
Add a reiniciar_conversacion tool that Claude can call when a client explicitly asks to start over, plus system prompt instructions that distinguish full-reset requests from simple "search again" requests.

Purpose: Without this tool, Claude can only verbally acknowledge a restart but the conversation memory persists, causing context bleed from previous flows into the new one. The tool clears both DB history and session property cache for a clean slate.

Output: New tool function + TOOLS_DEFINITION entry in tools.py, dispatcher entry in brain.py, restart flow block in prompts.yaml.
</objective>

<execution_context>
@C:/Users/Usuario/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Usuario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-business-flows/04-RESEARCH.md
@.planning/phases/04-business-flows/04-01-SUMMARY.md
@agent/tools.py
@agent/brain.py
@agent/memory.py
@agent/session.py
@config/prompts.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reiniciar_conversacion tool function and TOOLS_DEFINITION entry</name>
  <files>agent/tools.py</files>
  <action>
Add a new async function `reiniciar_conversacion(telefono: str) -> str` to agent/tools.py. Place it after the `obtener_propiedades_para_visita` function and before the `TOOLS_DEFINITION` list.

The function must:
1. Import limpiar_historial from agent.memory (use inline import to avoid circular deps, matching existing pattern in registrar_lead_ghl)
2. Call `await limpiar_historial(telefono)` to clear DB conversation history
3. Clear session property cache: import `_cache` from `agent.session` and delete the telefono key if present (to prevent stale property lists for agendar visita)
4. Log the action: `logger.info(f"Conversación reiniciada para {telefono}")`
5. Return: `"Historial limpiado exitosamente. El cliente puede empezar desde cero. Enviar la lista de calificación inicial inmediatamente."`

Also add the tool definition to the TOOLS_DEFINITION list:
```python
{
    "name": "reiniciar_conversacion",
    "description": (
        "Borra el historial de conversación completo para reiniciar el flujo de calificación "
        "desde cero. Llamar SOLO cuando el cliente pida explícitamente empezar de nuevo "
        "(ej: 'empezar de nuevo', 'empezar desde cero', 'olvidate de todo', 'me equivoqué quiero cambiar todo'). "
        "NO llamar para 'quiero buscar otra cosa' o 'ver algo diferente' — esos solo necesitan "
        "volver al paso correspondiente del flujo sin borrar historial. "
        "El teléfono del cliente está en el contexto interno del mensaje."
    ),
    "input_schema": {
        "type": "object",
        "properties": {
            "telefono": {
                "type": "string",
                "description": "Teléfono del cliente (del contexto interno)"
            }
        },
        "required": ["telefono"]
    }
}
```

Add the import of reiniciar_conversacion to the imports at the top of brain.py (next step handles dispatcher).
  </action>
  <verify>
1. `python -c "from agent.tools import reiniciar_conversacion, TOOLS_DEFINITION; print('OK'); print([t['name'] for t in TOOLS_DEFINITION])"` — imports without error and reiniciar_conversacion appears in tool names list
2. Check that TOOLS_DEFINITION now has 6 entries (was 5: buscar_propiedades, obtener_detalle_propiedad, registrar_lead_ghl, obtener_link_agendar, obtener_propiedades_para_visita + now reiniciar_conversacion)
  </verify>
  <done>
reiniciar_conversacion function exists in tools.py, clears both DB history and session cache, and is listed in TOOLS_DEFINITION with a description that explicitly distinguishes full-reset from soft-restart.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire reiniciar_conversacion in brain.py dispatcher and add restart flow to prompts.yaml</name>
  <files>agent/brain.py, config/prompts.yaml</files>
  <action>
**brain.py changes:**
1. Add `reiniciar_conversacion` to the import from agent.tools (line ~22-28 where other tools are imported)
2. Add an `elif` branch in `_ejecutar_herramienta()` for "reiniciar_conversacion":
   ```python
   elif nombre == "reiniciar_conversacion":
       return await reiniciar_conversacion(**parametros)
   ```
   Place it after the `obtener_propiedades_para_visita` branch and before the `enviar_botones`/`enviar_lista` branch.

**prompts.yaml changes:**
Add a "Reinicio de conversación" section to config/prompts.yaml. Place it AFTER the three new flow blocks added in plan 04-01, BEFORE the "## Horario" section.

Content (in Spanish, matching existing prompt style):
```
## Reinicio de conversación
Cuando el cliente pide EXPLÍCITAMENTE empezar de nuevo:
- Frases que activan reinicio: "empezar de nuevo", "empezar desde cero", "olvidate de todo", "me equivoqué quiero cambiar todo", "resetear", "borrar todo"
- Llamar a reiniciar_conversacion con el teléfono del contexto interno
- Responder: "¡Claro! Empezamos de nuevo 😊" e INMEDIATAMENTE enviar la lista interactiva del paso 1 (¿Qué necesitás?)

IMPORTANTE — NO reiniciar para estas frases:
- "quiero buscar otra cosa" → volver al paso 2 (tipo de propiedad) SIN borrar historial
- "ver algo diferente" → volver al paso correspondiente del flujo
- "cambiar la zona" → volver al paso 3 (zona) SIN borrar historial
- "cambiar el presupuesto" → volver al paso 4 (presupuesto) SIN borrar historial
Para estos casos, mantener el contexto y solo ajustar el paso específico del flujo de calificación.
```
  </action>
  <verify>
1. `python -c "from agent.brain import _ejecutar_herramienta; print('OK')"` — brain.py imports without error
2. Grep brain.py for "reiniciar_conversacion" — appears in both import and dispatcher
3. Grep prompts.yaml for "Reinicio de conversación" — section exists
4. Grep prompts.yaml for "NO reiniciar" — distinction between full reset and soft restart is documented
5. `python -c "import yaml; yaml.safe_load(open('config/prompts.yaml','r',encoding='utf-8')); print('YAML OK')"` — YAML parses correctly
  </verify>
  <done>
reiniciar_conversacion is wired end-to-end: Claude sees the tool in ALL_TOOLS (via TOOLS_DEFINITION), can call it, brain.py dispatches to the function in tools.py, and the system prompt tells Claude exactly when to use it (full reset) vs when NOT to use it (soft re-qualification).
  </done>
</task>

</tasks>

<verification>
1. Full import chain works: `python -c "from agent.brain import generar_respuesta; from agent.tools import reiniciar_conversacion, TOOLS_DEFINITION; print('All imports OK')"`
2. Tool count: TOOLS_DEFINITION has 6 entries (5 existing + reiniciar_conversacion)
3. ALL_TOOLS in brain.py includes reiniciar_conversacion (it's in TOOLS_DEFINITION which feeds ALL_TOOLS)
4. prompts.yaml has both the three flow blocks (from 04-01) AND the restart section
5. prompts.yaml YAML parsing: `python -c "import yaml; yaml.safe_load(open('config/prompts.yaml','r',encoding='utf-8')); print('OK')"`
6. Dispatcher has reiniciar_conversacion branch: grep brain.py for the elif
</verification>

<success_criteria>
- reiniciar_conversacion function in tools.py clears DB history + session cache
- TOOLS_DEFINITION includes reiniciar_conversacion with explicit trigger/non-trigger phrase list
- brain.py _ejecutar_herramienta dispatches reiniciar_conversacion calls
- prompts.yaml restart section distinguishes "empezar de nuevo" (full reset) from "buscar otra cosa" (soft re-qualification)
- All Python imports resolve without error
</success_criteria>

<output>
After completion, create `.planning/phases/04-business-flows/04-02-SUMMARY.md`
</output>
