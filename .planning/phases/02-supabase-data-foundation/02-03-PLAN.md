---
phase: 02-supabase-data-foundation
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - .planning/phases/02-supabase-data-foundation/n8n-refresh-workflow.json
autonomous: false

must_haves:
  truths:
    - "n8n workflow triggers hourly and calls /admin/refresh-properties"
    - "Properties updated in Supabase appear in bot responses within 1 hour (DATA-04)"
    - "Bot responds with complete property data (precio, dormitorios, banos, superficie, descripcion) from Supabase"
  artifacts:
    - path: ".planning/phases/02-supabase-data-foundation/n8n-refresh-workflow.json"
      provides: "n8n workflow JSON for hourly property refresh"
  key_links:
    - from: "n8n workflow"
      to: "/admin/refresh-properties"
      via: "HTTP Request node POSTing to FastAPI server"
      pattern: "admin/refresh-properties"
---

<objective>
Create the n8n hourly refresh workflow and verify the complete Phase 2 data pipeline end-to-end: scraper -> Supabase -> bot cache -> client response.

Purpose: Complete the data freshness requirement (DATA-04: new properties appear within 1 hour) and verify all Phase 2 success criteria with a human checkpoint.
Output: n8n workflow JSON + verified end-to-end data pipeline
</objective>

<execution_context>
@C:/Users/Usuario/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Usuario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-supabase-data-foundation/02-RESEARCH.md
@.planning/phases/02-supabase-data-foundation/02-01-SUMMARY.md
@.planning/phases/02-supabase-data-foundation/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create n8n hourly refresh workflow JSON</name>
  <files>.planning/phases/02-supabase-data-foundation/n8n-refresh-workflow.json</files>
  <action>
Create an n8n workflow JSON file that the user can import into their n8n instance.

The workflow architecture (per RESEARCH.md recommendation — keep scraping logic in Python, n8n is just a scheduler):

```
Schedule Trigger (every 1 hour)
  -> HTTP Request: POST {SERVER_URL}/admin/refresh-properties
     Headers: X-Admin-Token: {{$env.AGENTKIT_ADMIN_TOKEN}}
     Timeout: 120 seconds (scraping 80+ detail pages takes ~60s)
  -> IF: statusCode != 200
     -> Send notification (optional — log error)
```

The workflow JSON must include:
1. **Schedule Trigger node**: Runs every 1 hour. Cron expression: `0 * * * *` (top of every hour).
2. **HTTP Request node**:
   - Method: POST
   - URL: `={{$env.AGENTKIT_SERVER_URL}}/admin/refresh-properties` (user sets AGENTKIT_SERVER_URL env var in n8n, e.g. `https://their-app.up.railway.app`)
   - Headers: `X-Admin-Token: ={{$env.AGENTKIT_ADMIN_TOKEN}}`
   - Timeout: 120000 (ms)
   - Response format: JSON
3. **IF node**: Check if HTTP status is 200. On failure path, log to console (no-op placeholder — user can add Telegram/email notification).

Include a comment in the workflow description explaining:
- User must set `AGENTKIT_SERVER_URL` and `AGENTKIT_ADMIN_TOKEN` as n8n environment variables
- The workflow calls the FastAPI server which handles all scraping logic
- Expected response: `{"status": "ok", "stats": {"total": N, "nuevas": M, "removidas": R}}`

Use standard n8n workflow JSON format with proper node IDs, positions, and connections. The workflow name should be "AgentKit - Refresh Propiedades (Hourly)".
  </action>
  <verify>
  - `python -c "import json; f=open('.planning/phases/02-supabase-data-foundation/n8n-refresh-workflow.json'); w=json.load(f); assert 'nodes' in w; assert len(w['nodes']) >= 2; print(f'Workflow has {len(w[\"nodes\"])} nodes')"` succeeds
  - The JSON is valid and contains Schedule Trigger + HTTP Request nodes
  </verify>
  <done>n8n workflow JSON exists and can be imported. Contains Schedule Trigger (hourly) + HTTP Request to /admin/refresh-properties with auth header.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete Phase 2 data pipeline end-to-end</name>
  <files>none</files>
  <action>
Human verifies the complete Phase 2 data pipeline works end-to-end.

Pre-requisites (one-time setup):
1. Create Supabase project at supabase.com (if not already done)
2. Run scripts/create_propiedades_table.sql in Supabase SQL Editor
3. Copy SUPABASE_URL and SUPABASE_KEY (anon key) from Supabase Dashboard -> Settings -> API
4. Add them to .env: SUPABASE_URL=https://xxx.supabase.co and SUPABASE_KEY=eyJ...
5. Set ADMIN_TOKEN in .env to any secret string (e.g. ADMIN_TOKEN=my-secret-123)
6. Run pip install -r requirements.txt to install supabase SDK

Test 1 - Initial scrape + persist:
  curl -X POST http://localhost:8000/admin/refresh-properties -H "X-Admin-Token: my-secret-123"
  Expected: {"status": "ok", "stats": {"total": 60+, "nuevas": 60+, "removidas": 0}}

Test 2 - Verify Supabase has data:
  Go to Supabase Dashboard -> Table Editor -> propiedades
  Verify 60+ rows exist with dormitorios, banos, sup_cubierta filled

Test 3 - Bot responds from cache:
  python tests/test_local.py
  Ask: "Busco departamentos en venta en Nueva Cordoba"
  Verify response comes in under 1 second with property details

Test 4 - Server warm-up:
  Restart server and check logs for "Cache de propiedades cargado desde Supabase"

Test 5 (optional) - n8n workflow:
  Import n8n-refresh-workflow.json into n8n and test execute
  </action>
  <verify>All 5 Phase 2 success criteria pass: full detail data shown, sub-1-second response, hourly refresh mechanism, correct prices, warm cache on startup</verify>
  <done>Phase 2 data pipeline verified end-to-end by human. Bot serves properties from Supabase with complete data.</done>
</task>

</tasks>

<verification>
Phase 2 success criteria check:
1. Bot shows precio, dormitorios, banos, superficie, descripcion (full detail data from Supabase)
2. Property search takes less than 1 second (from cache, not live scraping)
3. New properties on Bertero website appear in bot within 1 hour (n8n hourly refresh)
4. Price shown matches detail page price exactly (DATA-05)
5. On server startup, properties are already loaded in cache (TECH-07 warm-up)
</verification>

<success_criteria>
- n8n workflow JSON exists and is importable
- Full data pipeline works end-to-end: scrape -> Supabase -> cache -> bot response
- All 5 Phase 2 success criteria verified by human
</success_criteria>

<output>
After completion, create `.planning/phases/02-supabase-data-foundation/02-03-SUMMARY.md`
</output>
