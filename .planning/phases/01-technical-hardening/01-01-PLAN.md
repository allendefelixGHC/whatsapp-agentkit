---
phase: 01-technical-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent/utils.py
  - agent/dedup.py
  - agent/main.py
  - agent/memory.py
  - agent/ghl.py
  - requirements.txt
autonomous: true

must_haves:
  truths:
    - "Cuando Whapi reintenta un webhook, el bot no responde dos veces al mismo mensaje"
    - "El bot recuerda los ultimos 16 mensajes de cada conversacion (no solo 6)"
    - "Un mismo numero de telefono en distintos formatos (+54, 54, 0, sin prefijo) se resuelve siempre al mismo contacto"
  artifacts:
    - path: "agent/utils.py"
      provides: "Funcion centralizada normalizar_telefono()"
      contains: "def normalizar_telefono"
    - path: "agent/dedup.py"
      provides: "Cache TTL para deduplicacion de mensaje_id"
      contains: "def es_duplicado"
    - path: "agent/memory.py"
      provides: "Historial ampliado a 16 mensajes por defecto"
      contains: "limite: int = 16"
  key_links:
    - from: "agent/main.py"
      to: "agent/dedup.py"
      via: "import es_duplicado, called before Claude API"
      pattern: "es_duplicado\\(msg\\.mensaje_id\\)"
    - from: "agent/main.py"
      to: "agent/utils.py"
      via: "import normalizar_telefono for memory lookups"
      pattern: "normalizar_telefono"
    - from: "agent/ghl.py"
      to: "agent/utils.py"
      via: "import normalizar_telefono replacing inline logic"
      pattern: "from agent\\.utils import normalizar_telefono"
---

<objective>
Add message deduplication, centralized phone normalization, and expand conversation history from 6 to 16 messages.

Purpose: Prevent duplicate bot responses on webhook retries (TECH-02), eliminate inconsistent phone handling that creates duplicate contacts (TECH-03), and give Claude more conversation context for better responses (TECH-01).
Output: Three new/modified modules providing reliable message processing foundation.
</objective>

<execution_context>
@C:/Users/Usuario/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Usuario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-technical-hardening/01-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@agent/main.py
@agent/memory.py
@agent/ghl.py
@agent/session.py
@requirements.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create phone normalization utility and message deduplication cache</name>
  <files>agent/utils.py, agent/dedup.py, requirements.txt</files>
  <action>
1. Add `cachetools>=5.3.0` to `requirements.txt` (append after existing deps). Run `pip install cachetools`.

2. Create `agent/utils.py` with function `normalizar_telefono(raw: str) -> str`:
   - Strip `@s.whatsapp.net`, `@c.us` suffixes via `re.sub(r"@.*", "", raw)`
   - Strip all non-digit chars via `re.sub(r"[^\d]", "", s)`
   - Remove leading trunk prefix "0" (Argentina)
   - Remove leading "15" mobile prefix ONLY if remaining length <= 10
   - If 10 digits (local AR without country code): prepend "549"
   - If starts with "54" but not "549" and length == 12: insert "9" after "54" (mobile indicator)
   - Return digits-only string
   - Follow the exact implementation from 01-RESEARCH.md Pattern 1
   - Add docstring with examples showing all input variants

3. Create `agent/dedup.py` with:
   - Module-level `_seen: TTLCache = TTLCache(maxsize=10_000, ttl=300)` (5 minutes TTL)
   - Function `es_duplicado(mensaje_id: str) -> bool`:
     - If `mensaje_id` is empty/falsy, return False (never dedup empty IDs)
     - If `mensaje_id` in `_seen`, return True
     - Otherwise set `_seen[mensaje_id] = True` and return False
   - Add logger warning when empty mensaje_id is received
   - Follow conventions: `import logging`, `logger = logging.getLogger("agentkit")`
  </action>
  <verify>
Run `python -c "from agent.utils import normalizar_telefono; assert normalizar_telefono('5493517575244@s.whatsapp.net') == '5493517575244'; assert normalizar_telefono('+543517575244') == '5493517575244'; assert normalizar_telefono('03517575244') == '5493517575244'; assert normalizar_telefono('3517575244') == '5493517575244'; print('OK')"` and `python -c "from agent.dedup import es_duplicado; assert es_duplicado('msg1') == False; assert es_duplicado('msg1') == True; assert es_duplicado('') == False; print('OK')"`
  </verify>
  <done>normalizar_telefono handles all Argentina phone formats returning 13-digit canonical form. es_duplicado correctly identifies repeated message IDs within TTL window. cachetools installed.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate dedup and phone normalization into main.py and ghl.py, expand history to 16</name>
  <files>agent/main.py, agent/memory.py, agent/ghl.py</files>
  <action>
1. **agent/memory.py** — Change `obtener_historial` default parameter from `limite: int = 6` to `limite: int = 16`. Also fix the docstring which says "default: 20" but was actually 6.

2. **agent/main.py** — In `webhook_handler`, add dedup check inside the `for msg in mensajes` loop, right after the `if msg.es_propio` skip check:
   ```python
   from agent.dedup import es_duplicado
   # ... inside loop, after es_propio check:
   if es_duplicado(msg.mensaje_id):
       logger.debug(f"Mensaje duplicado ignorado: {msg.mensaje_id}")
       continue
   ```

3. **agent/main.py** — In `webhook_handler`, normalize the phone for memory operations. Add `from agent.utils import normalizar_telefono` at top. Use `telefono_normalizado = normalizar_telefono(msg.telefono)` for `obtener_historial()` and `guardar_mensaje()` calls. IMPORTANT: Keep `msg.telefono` (original with @s.whatsapp.net) for `proveedor.enviar_respuesta()` and `proveedor.enviar_indicador_tipeo()` — those need the original Whapi format.

4. **agent/main.py** — In `ghl_webhook_handler`, replace the inline phone normalization (lines 246-250 where `tel_limpio = phone.replace("+", "")` and the `startswith("54")` fix) with `normalizar_telefono(phone)`. Then append `@s.whatsapp.net` for the Whapi send: `tel_whapi = normalizar_telefono(phone) + "@s.whatsapp.net"`.

5. **agent/ghl.py** — Replace the inline phone normalization in `_formatear_telefono_ghl()` (around line 113 where it does `tel_limpio = telefono.replace("@s.whatsapp.net", "")...`) with a call to `normalizar_telefono()` from `agent.utils`. The GHL format needs `+` prefix, so: `return "+" + normalizar_telefono(telefono)`. Also replace the inline normalization in `buscar_contacto_por_telefono()` (around line 422) with the same approach.
  </action>
  <verify>
Run `python -c "from agent.memory import obtener_historial; import inspect; sig = inspect.signature(obtener_historial); assert sig.parameters['limite'].default == 16; print('History OK')"` and `python -c "import ast; tree = ast.parse(open('agent/main.py').read()); print('Syntax OK')"` and `python -c "import ast; tree = ast.parse(open('agent/ghl.py').read()); print('Syntax OK')"` to verify all files parse correctly. Then start the server with `timeout 5 uvicorn agent.main:app --port 8111 2>&1 || true` to confirm it boots without import errors.
  </verify>
  <done>Dedup blocks repeated mensaje_id before Claude API call. Phone normalization is centralized — ghl.py and main.py both call normalizar_telefono() instead of inline logic. History default is 16 messages. Server boots cleanly.</done>
</task>

</tasks>

<verification>
1. Import all new modules: `python -c "from agent.utils import normalizar_telefono; from agent.dedup import es_duplicado; print('Imports OK')"`
2. Verify no inline phone normalization remains in ghl.py or main.py (search for `replace("+", "")` patterns related to phone)
3. Server starts: `uvicorn agent.main:app --port 8111` boots without errors
4. Dedup prevents duplicate processing: es_duplicado returns True on second call with same ID
5. Phone normalization handles all formats: +54, 549, 0, @s.whatsapp.net
</verification>

<success_criteria>
- cachetools is in requirements.txt and installed
- agent/utils.py exists with normalizar_telefono() handling all AR phone formats
- agent/dedup.py exists with es_duplicado() using TTLCache(maxsize=10000, ttl=300)
- agent/memory.py has limite=16 as default
- agent/main.py calls es_duplicado() before processing and normalizar_telefono() for memory ops
- agent/ghl.py uses normalizar_telefono() instead of inline logic
- No duplicate phone normalization code exists outside agent/utils.py
- Server starts without import errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-technical-hardening/01-01-SUMMARY.md`
</output>
