---
phase: 05-human-takeover
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent/memory.py
  - agent/takeover.py
  - agent/main.py
  - .env.example
autonomous: true

must_haves:
  truths:
    - "Cada conversacion tiene un estado persistente en la DB: bot, humano, o cerrado (HT-01)"
    - "Cuando el estado es humano, el bot NO responde a mensajes de ese cliente (HT-04)"
    - "El estado sobrevive reinicios del servidor (persiste en SQLAlchemy)"
  artifacts:
    - path: "agent/memory.py"
      provides: "ConversationState SQLAlchemy model"
      contains: "class ConversationState"
    - path: "agent/takeover.py"
      provides: "State CRUD functions: obtener_estado, set_estado"
      exports: ["obtener_estado", "set_estado"]
    - path: "agent/main.py"
      provides: "State gate before generar_respuesta"
      contains: "obtener_estado"
    - path: ".env.example"
      provides: "VENDEDOR_WHATSAPP and TAKEOVER_TIMEOUT_HOURS env vars"
      contains: "VENDEDOR_WHATSAPP"
  key_links:
    - from: "agent/main.py"
      to: "agent/takeover.py"
      via: "obtener_estado() call in webhook handler"
      pattern: "obtener_estado\\(telefono"
    - from: "agent/takeover.py"
      to: "agent/memory.py"
      via: "imports async_session and ConversationState"
      pattern: "from agent\\.memory import"
---

<objective>
Create the conversation state foundation for human takeover: a ConversationState SQLAlchemy model, CRUD functions in a new takeover.py module, and a state gate in main.py that silences the bot when a conversation is in "humano" mode.

Purpose: Without state management and the gate, none of the other HT features can work. This plan establishes the DB table and the critical "bot stays silent" behavior.
Output: ConversationState table auto-created on startup, obtener_estado/set_estado functions, bot skips Claude entirely for humano conversations.
</objective>

<execution_context>
@C:/Users/Usuario/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Usuario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-human-takeover/05-RESEARCH.md
@agent/memory.py
@agent/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: ConversationState model + takeover.py CRUD + env vars</name>
  <files>agent/memory.py, agent/takeover.py, .env.example</files>
  <action>
1. In agent/memory.py, add the ConversationState model AFTER the existing Mensaje class (same Base):
   - __tablename__ = "conversation_states"
   - id: Integer primary key autoincrement
   - telefono: String(50), unique=True, index=True
   - estado: String(20), default="bot" — valid values: "bot", "humano", "cerrado"
   - vendedor: String(100), default=""
   - updated_at: DateTime, default=datetime.utcnow
   - The existing `inicializar_db()` already calls `Base.metadata.create_all` so ConversationState table will be auto-created.

2. Create agent/takeover.py with these functions:
   - `async def obtener_estado(telefono: str) -> str`: Query ConversationState by telefono. Return estado if found, "bot" if no row exists. Use async_session from memory.py.
   - `async def set_estado(telefono: str, estado: str, vendedor: str = "") -> None`: Upsert — if row exists for telefono, update estado + vendedor + updated_at. If not, insert new row. Use merge pattern or select+update/insert.
   - `def construir_mensaje_vendedor(cliente_telefono: str, resumen: str) -> str`: Build the WhatsApp notification text for the vendor (plain text with WhatsApp bold formatting). Format per research Pattern 6:
     ```
     *TAKEOVER — Cliente solicita asesor*

     *Cliente:* {cliente_telefono}

     *Resumen:*
     {resumen}

     ---
     Para devolver al bot:
     #bot {cliente_telefono}
     ```
   - Import from memory.py: `async_session`, `ConversationState`
   - Import logging, os, datetime

3. Append to .env.example:
   ```
   # -- Human Takeover ------------------------------------------
   # Telefono del vendedor en formato internacional (sin + ni @s.whatsapp.net)
   # El bot envia notificacion WhatsApp a este numero cuando un cliente pide hablar con alguien
   # Ej: 5493517575244 para Argentina +549 (351) 757-5244
   VENDEDOR_WHATSAPP=
   # Horas de inactividad antes de devolver automaticamente una conversacion al bot (default 4)
   TAKEOVER_TIMEOUT_HOURS=4
   ```
  </action>
  <verify>
  Run `python -c "from agent.memory import ConversationState; print(ConversationState.__tablename__)"` — should print "conversation_states".
  Run `python -c "from agent.takeover import obtener_estado, set_estado, construir_mensaje_vendedor; print('OK')"` — should print "OK".
  </verify>
  <done>ConversationState model exists in memory.py, takeover.py exports obtener_estado/set_estado/construir_mensaje_vendedor, .env.example has VENDEDOR_WHATSAPP and TAKEOVER_TIMEOUT_HOURS.</done>
</task>

<task type="auto">
  <name>Task 2: State gate in main.py webhook handler</name>
  <files>agent/main.py</files>
  <action>
Insert the state gate in the webhook_handler POST /webhook function. The gate goes AFTER the rate limit check (line ~163 `continue`) and BEFORE the log/typing indicator/historial block (line ~166).

Add import at top of main.py:
```python
from agent.takeover import obtener_estado
```

Insert this block after the rate limit check and before the "Log con contexto de interaccion" comment:

```python
# Human takeover gate (HT-04): si la conversacion esta en modo "humano",
# el bot NO responde — silencio total, sin guardar en historial
estado_conv = await obtener_estado(telefono_normalizado)
if estado_conv == "humano":
    logger.info(f"Conversacion {telefono_normalizado} en modo HUMANO — bot en pausa")
    continue  # Saltar completamente — no llamar a Claude, no responder, no guardar
```

CRITICAL: The gate must use `telefono_normalizado` (already computed above from normalizar_telefono). The `continue` skips the entire message processing including generar_respuesta, guardar_mensaje, and enviar_respuesta. This is intentional — messages during human mode are NOT saved to bot history per research Pitfall 5.

Also add a startup warning in lifespan() if VENDEDOR_WHATSAPP is not set:
```python
vendedor_wa = os.getenv("VENDEDOR_WHATSAPP", "")
if not vendedor_wa:
    logger.warning("VENDEDOR_WHATSAPP no configurado — human takeover notifications disabled")
```
  </action>
  <verify>
  Run `python -c "from agent.main import app; print('Server loads OK')"` — should print without import errors.
  Grep agent/main.py for "obtener_estado" — should find the import and the gate call.
  Grep agent/main.py for "modo HUMANO" — should find the log line.
  </verify>
  <done>main.py webhook handler checks conversation state before calling Claude. If estado=="humano", the message is silently skipped (no response, no history save). VENDEDOR_WHATSAPP warning logged at startup if not set.</done>
</task>

</tasks>

<verification>
1. `python -c "from agent.memory import ConversationState; from agent.takeover import obtener_estado, set_estado; print('imports OK')"` passes
2. `python -c "from agent.main import app; print('server OK')"` passes (no import errors)
3. grep "estado_conv" agent/main.py shows the gate in the webhook handler
4. grep "VENDEDOR_WHATSAPP" .env.example shows the new env var
</verification>

<success_criteria>
- ConversationState model added to memory.py with telefono unique index
- takeover.py module created with obtener_estado, set_estado, construir_mensaje_vendedor
- main.py webhook handler has state gate that silences bot when estado=="humano"
- .env.example has VENDEDOR_WHATSAPP and TAKEOVER_TIMEOUT_HOURS
- Server starts without errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-human-takeover/05-01-SUMMARY.md`
</output>
