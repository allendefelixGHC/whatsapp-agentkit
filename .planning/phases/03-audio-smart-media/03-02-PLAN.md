---
phase: 03-audio-smart-media
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - config/prompts.yaml
autonomous: true

must_haves:
  truths:
    - "Cuando un cliente envia foto de una propiedad sin texto, el bot lanza buscar_propiedades automaticamente basandose en lo que ve en la imagen"
    - "Cuando un cliente envia foto de un cartel de Bertero, el bot identifica la propiedad y ofrece agendar visita directamente"
    - "Cuando un cliente envia una foto que no es de inmuebles (selfie, documento), el bot NO lanza busqueda sino que pregunta que necesita"
    - "Cuando no hay coincidencia exacta del cartel Bertero, el bot muestra propiedades similares de la zona y ofrece visita"
  artifacts:
    - path: "config/prompts.yaml"
      provides: "System prompt with proactive image search and Bertero sign detection instructions"
      contains: "buscar_propiedades"
  key_links:
    - from: "config/prompts.yaml"
      to: "agent/brain.py"
      via: "System prompt loaded by cargar_system_prompt() instructs Claude to call buscar_propiedades on image receipt"
      pattern: "buscar_propiedades"
---

<objective>
Update the system prompt so Claude proactively triggers property search when receiving images, and detects Bertero signs to offer visit booking directly.

Purpose: Currently when a client sends a photo, the bot asks what they need instead of acting on what it sees. This creates unnecessary friction. The bot should analyze the image and immediately search for matching properties (IMG-01), and when it detects a Bertero sign, offer to book a visit directly (IMG-02). No code changes needed -- this is purely system prompt engineering leveraging the existing Claude Vision capability.

Output: Updated config/prompts.yaml with smart image processing instructions.
</objective>

<execution_context>
@C:/Users/Usuario/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Usuario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-audio-smart-media/03-RESEARCH.md
@config/prompts.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite image processing section in system prompt for proactive search</name>
  <files>config/prompts.yaml</files>
  <action>
  In `config/prompts.yaml`, REPLACE the existing "## Fotos de propiedades" section (lines ~104-111) with a new, more assertive version that instructs Claude to:

  **Section 1: "## Procesamiento de imagenes (CRITICO)"**

  When the client sends a PHOTO without caption (message is "[Imagen enviada]"):
  1. ANALYZE the image immediately with full visual capability
  2. FIRST assess: does the image show a property, real estate, or a "for sale/rent" sign? If NOT (selfie, document, screenshot, unrelated photo), DO NOT search -- acknowledge the image and ask what they need: "Recibi tu foto pero no parece ser de un inmueble. ¿En que puedo ayudarte?"
  3. If it IS real estate related: identify property type (casa, depto, terreno, local, galpon, oficina), zone/neighborhood if visible from signs or recognizable landmarks, operation type if a sign says "venta"/"alquiler", and any other visible details (number of floors, approximate size, condition)
  4. IMMEDIATELY call `buscar_propiedades` with the detected parameters -- DO NOT ask the client additional questions first. Launch the search directly with what you can see.
  5. Present results naturally: "Vi que me mandaste una foto de [lo que ves]. Te busque propiedades similares en nuestro catalogo:"

  When the client sends a PHOTO WITH caption:
  - Use the caption text as the primary input, but also analyze the image to enrich the search parameters
  - If the caption asks about the property in the photo, use image analysis to identify type/zone and search

  **Section 2: "## Deteccion de carteles Bertero (CRITICO)"**

  If the image shows a BERTERO SIGN (look for: "Bertero" text, Bertero logo, corporate colors, "Bertero Negocios Inmobiliarios"):
  1. Identify any property code, address, phone number, or reference visible on the sign
  2. Call `buscar_propiedades` with any identifiable data (address as zone, property type if visible)
  3. If you found a matching property: present it and DIRECTLY offer to book a visit:
     "Encontre esa propiedad! [property details]. ¿Queres que te coordine una visita?"
     Then use `enviar_botones` with: "Agendar visita"(id:btn_agendar_visita) / "Mas info"(id:btn_ver_mas)
  4. If NO exact match found: show similar properties from the same zone and offer a visit for the closest match:
     "No encontre esa propiedad exacta, pero tenemos estas opciones similares en la zona:"
     Then present the results and offer to book a visit for any of them.
  5. If the sign is from ANOTHER real estate agency (not Bertero): "Esa propiedad parece ser de otra inmobiliaria, pero puedo buscar algo similar en nuestro catalogo"

  **CRITICAL SAFETY RULES for image processing (include these explicitly):**
  - NEVER invent property details not visible in the image
  - If you cannot determine the property type, default to searching without type filter
  - If you cannot determine the zone, search across all zones
  - If the image is blurry or unclear, acknowledge it: "La foto no se ve muy bien, ¿podes decirme la zona o que tipo de propiedad estas buscando?"
  - ALWAYS use `buscar_propiedades` for actual data -- never make up prices, addresses, or availability

  Keep ALL other sections of prompts.yaml EXACTLY as they are. Only replace the "## Fotos de propiedades" section. The "## Links de portales inmobiliarios" section that follows must remain untouched.
  </action>
  <verify>
  - `python -c "import yaml; d=yaml.safe_load(open('config/prompts.yaml','r',encoding='utf-8')); p=d['system_prompt']; assert 'Procesamiento de imagenes' in p or 'Procesamiento de imágenes' in p; assert 'buscar_propiedades' in p; assert 'Deteccion de carteles Bertero' in p or 'Detección de carteles Bertero' in p; print('prompts.yaml OK')"` passes
  - `grep -c "buscar_propiedades" config/prompts.yaml` returns at least 4 (existing uses + new image instructions)
  - The "## Links de portales inmobiliarios" section is still present: `grep "Links de portales" config/prompts.yaml` returns a match
  - The fallback_message and error_message at the bottom are still present: `grep "fallback_message" config/prompts.yaml` returns a match
  - YAML is valid: `python -c "import yaml; yaml.safe_load(open('config/prompts.yaml','r',encoding='utf-8')); print('YAML valid')"` passes
  - No-match fallback exists: `grep -i "similares en la zona" config/prompts.yaml` returns a match
  </verify>
  <done>System prompt instructs Claude to proactively search for properties when receiving images, detect Bertero signs and offer visits (including fallback to similar properties when no exact match found), and gracefully handle non-property images without launching unnecessary searches.</done>
</task>

</tasks>

<verification>
1. YAML is valid: `python -c "import yaml; yaml.safe_load(open('config/prompts.yaml','r',encoding='utf-8')); print('OK')"`
2. Image processing section exists: `python -c "import yaml; d=yaml.safe_load(open('config/prompts.yaml','r',encoding='utf-8')); assert 'Procesamiento de' in d['system_prompt']; print('OK')"`
3. Bertero sign detection exists: `python -c "import yaml; d=yaml.safe_load(open('config/prompts.yaml','r',encoding='utf-8')); assert 'carteles Bertero' in d['system_prompt']; print('OK')"`
4. Non-property image safety: `grep -c "selfie\|documento\|screenshot" config/prompts.yaml` returns >= 1
5. Rest of prompt unchanged: `grep "Links de portales" config/prompts.yaml` returns a match
6. No-match Bertero fallback: `grep -i "similares" config/prompts.yaml` returns a match for the zone-based fallback
</verification>

<success_criteria>
- System prompt contains explicit instructions for proactive property search on image receipt
- System prompt contains Bertero sign detection instructions with visit booking offer
- System prompt contains fallback behavior when no exact Bertero match found (show similar properties from same zone)
- System prompt contains safety rules to avoid searching on non-property images
- The rest of the system prompt (qualification flow, lead registration, scheduling, rules) is unchanged
- YAML file is valid and parseable
</success_criteria>

<output>
After completion, create `.planning/phases/03-audio-smart-media/03-02-SUMMARY.md`
</output>
